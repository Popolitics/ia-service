DJANGO_SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
# DATABASE_URL=postgres://user:password@localhost:5432/ia_service

# Clé PUBLIQUE d'auth-service, pour vérifier ses JWT (RS256). Jamais la clé
# privée : ia-service ne signe rien, il ne fait que valider.
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

# TODO: config Mistral (clé API, modèle) — à ajouter quand l'intégration
# IA sera câblée. Rien de branché pour l'instant, ce scaffold est le socle
# Django nu.
